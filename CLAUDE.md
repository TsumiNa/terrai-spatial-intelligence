# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**All guidance lives in [AGENTS.md](AGENTS.md). Read it before making any change.**

This file is intentionally a pointer only — it holds no rules of its own. Never add repository guidance here; add it to `AGENTS.md` so there is exactly one file to maintain.
