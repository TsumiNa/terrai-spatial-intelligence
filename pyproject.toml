[project]
name = "terrai-spatial-demo"
version = "0.3.0"
description = "Auditable multilingual TerrAI spatial intelligence PoC"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
remote = [
  "numpy>=2.0",
  "pillow>=10.0",
  "pyproj>=3.6",
  "rasterio>=1.3",
]

[tool.uv]
package = false
default-groups = []
